# FS-ASM — repository handoff / jeden bieżący punkt statusowy

**Aktualizacja dokumentacji:** 17.09.2026. **Zweryfikowany punkt odniesienia:** `Fsasm-experimental` @ `d1fb7ac34bf204cdd662a51a12ba4ece3138e4cd` (merge PR #34, T11 Tool Broker dla operacji plikowych; T10 scalone jako `ff118da`; T09 scalone jako `86a503f` całego planu i agregacja Parent/run; Gate C osiągnięta na `56d1997`, T08 scalone jako `76cb8c4`). To datowany snapshot, nie gwarancja aktualnego HEAD. Każda kolejna sesja weryfikuje żywy HEAD, PR i CI przed rozpoczęciem zadania.

## Cel i uprawnienia

[Zatwierdzona architektura v1](../../../../fsasm-first/docs/FSASM_ARCHITEKTURA_RUNTIME_V1_ZATWIERDZONA_2026-09-17.md) jest jedynym kontraktem celu (SHA-256 `56aeeb60c8c700c71dc3d395659127afbf511e1726e0ffd76270c848fd764808`). [Decyzje wdrożeniowe](../../../../fsasm-first/docs/DEPLOYMENT_DECISIONS.md) doprecyzowują profil hybrydowy Workflows i zgodę na jawne dane/telemetrię. `Fsasm-experimental` jest linią integracyjną; `main` pozostaje osobno. Użytkownik + ChatGPT planują/review, Vibe Code Web pisze kod. Nie zmieniaj kanonu, kodu lub milestone'ów na podstawie samego handoffu.

## Potwierdzone na wskazanym punkcie odniesienia

- [PR #23](https://github.com/bmateuszideas/fsasm-training-lab/pull/23) (T00) scalony do `Fsasm-experimental` 17.09.2026; merge SHA `fae7bcf`. Kanoniczny TODO i pojedynczy handoff są w linii integracyjnej.
- [PR #24](https://github.com/bmateuszideas/fsasm-training-lab/pull/24) (T01) scalony 17.09.2026; merge SHA `de302b5`. T01 niezależnie odtworzyło G3, G4 i G5 na HEAD `a470cdd` — wszystkie `PRESENT` (G3: legacy consent reassignable across gates; G4/G5: rejection attribution/drain). Raport reprodukcji przypięty do SHA; bez zmian kodu produkcyjnego.
- [PR #25](https://github.com/bmateuszideas/fsasm-training-lab/pull/25) (T02) scalony 17.09.2026; merge SHA `31d6724`. T02 naprawiło G4/G5 (atrybucja i drain odrzuceń Human Gate) bez regresji RETRY_ONCE/ABORT; późne rejection nie ginie, `None` pozostaje `None`.
- [PR #26](https://github.com/bmateuszideas/fsasm-training-lab/pull/26) (T03) scalony 17.09.2026; merge SHA `a7788a1`. T03 naprawiło G3 (tożsamość zgody Human Gate): runtime wymaga pełnych `run_id`/`task_id`/`gate_id`/`decision_id` na granicy, odrzuca niepełny/obcy/stary/przyszły payload jako `incomplete_payload` przed rezerwacją bramki, zachowuje first-valid-wins i stabilny `decision_id`. **Gate AB osiągnięta:** T00–T03 ACCEPTED, brak warunkowych NOT REQUIRED; pełny pytest zielony na `a7788a1`.
- [PR #27](https://github.com/bmateuszideas/fsasm-training-lab/pull/27) (T04) scalony 17.09.2026; merge SHA `a283bb8`. T04 dodało kompletny model domenowy snapshotu: `TaskBudget`/`RunBudget`/`GateOccurrence`, pola `schema_version`/`revision`/`needs_human_task_ids`/`gate`/`budget`, `allowed_tools`/`accepted_evidence_refs` na `ChildTask`, walidator `validate_register_invariants` (leniwy o stany przejściowe M4), detekcję cykli i 7 deterministycznych helperów tożsamości. `Plan.validate_tasks` rozluźnione z „dokładnie 3” na „min 1”. Brak regresji (660 passed, 3 skipped).
- [PR #28](https://github.com/bmateuszideas/fsasm-training-lab/pull/28) (T05) scalony 17.09.2026; merge SHA `082d13a`. T05 przeniosło macierze przejść/limitów z `transitions.py` do nowego `src/fsasm/domain.py` (jeden autorytet; `transitions.py` re-używa je przez aliasy, API M4 bez zmian); zdefiniowało 13 jawnych zdarzeń domenowych i czyste `apply_event(current_state, event) -> next_state` bez I/O i bez mutacji wejścia. Tylko Domain Core zatwierdza PASS/retry/gate; PASS wymaga `verification_passed=True` + niepustych zaakceptowanych evidence aktualnej próby; jeden aktywny task; `revision` celowo niezmienione (T06). Brak regresji (697 passed, 3 skipped).
- [PR #29](https://github.com/bmateuszideas/fsasm-training-lab/pull/29) (T06) scalony 17.09.2026; merge SHA `56d1997`. T06 dodało `src/fsasm/state_repository.py` (`StateRepository` + `StaleRevisionError`) komponujące `RuntimePersistence` (F3/F4/F5 bez zmian dla M1–M4): `load(run_id)`, `init_snapshot(state)` (revision 0, ścieżka `start_new`) oraz `commit_snapshot(run_id, expected_revision, next_state)` — optymistyczna współbieżność odrzuca stale `expected_revision` (G1), odrzuca niespójny `run_id`, inkrementuje revision raz; `state.json` autorytet zapisywany najpierw, `plan.json` (projekcja) po nim; fault injection dowodzi, że awaria projekcji nie cofa autorytetu i nie daje false PASS. Brak regresji (718 passed, 3 skipped).
- **Gate C osiągnięta:** T04–T06 ACCEPTED, pełny pytest zielony na `56d1997`, kontrolny restart na stubie (nowy `StateRepository`/`RuntimeLifecycle` nad tym samym dyskiem) wraca do zatwierdzonego snapshotu (jeden snapshot = plan + Task Register + gate + counters + evidence refs; jedna czysta operacja `apply_event` + jeden commit; brak drugiego źródła statusów).
- [PR #30](https://github.com/bmateuszideas/fsasm-training-lab/pull/30) (T07) scalony 17.09.2026; merge SHA `b04b1f8`. T07 dodało `src/fsasm/runtime_lifecycle.py` (`RuntimeLifecycle.start_new` rezerwuje NOWY run F4 i pisze pierwszy snapshot revision 0, odmawia nadpisania istniejącego runu/celu; `resume` ładuje i waliduje zatwierdzony snapshot, zwraca `(state, revision)`, nie obiecuje reconcile — T23) oraz `StateRepository.advance(run_id, expected_revision, event)` (adapter activities `load → apply_event → commit_snapshot`); nieaktualna activity (stale `expected_revision`) odrzucona i nie nadpisuje nowszego stanu; technical retry (zdarzenie nie-attempt) nie zwiększa `task_attempt`. M1–M4 nadal używają imperatywnej ścieżki `commit_run_state` (nie usuwamy jej do T27). Brak regresji (735 passed, 3 skipped).
- [PR #31](https://github.com/bmateuszideas/fsasm-training-lab/pull/31) (T08) scalony 17.09.2026; merge SHA `76cb8c4`. T08 dodało `src/fsasm/task_compiler.py` (`compile_plan(goal_input, proposal) -> Plan` — jedyna granica zamieniająca niezaufany `PlannerProposal` w runtime-owned `Plan`; nadaje runtime-owned IDs, Parent/Child, zależności, statusy, próby, scope i limity kapowane przez `GoalInput.constraints` jako jedyny źródło uprawnień; odrzuca pustą propozycję, plan-size overflow, duplikaty, złe/brakujące/cykliczne zależności, rozszerzenie uprawnień i nieznany verification_type PRZED utworzeniem runu; zwraca czysty `Plan` bez typów transportowych). `GoalInput` rozszerzone o `RunConstraints`; `PlannerProposal` rozluźnione z „dokładnie 3” na „min 1” (semantyczne wejście). M1–M4 bez zmian. Brak regresji (765 passed, 3 skipped).
- [PR #32](https://github.com/bmateuszideas/fsasm-training-lab/pull/32) (T09) scalony 17.09.2026; merge SHA `86a503f`. T09 dodało `src/fsasm/scheduler.py` (`schedule(state) -> ScheduleResult` — czyste zapytanie nad `RunState`, bez mutacji/I/O/nadawania PASS; wybiera deterministycznie kwalifikujący się Child Task (najniższy sequence, tie-break task_id) po spełnieniu wszystkich zależności; utrzymuje najwyżej jeden aktywny task — `BUSY` gdy `active_task_id` ustawione; rozróżnia `ALL_COMPLETE` / `BLOCKED` / `WAITING_ON_GATE` (run NEEDS_HUMAN) / `BUSY`; `all_required_tasks_complete` liczy Parent/run ze wszystkich wymaganych Child Tasks — PASS TASK-001 NIE kończy planu wielozadaniowego). M1–M4 `find_next_ready_task_activity` (hardcoded TASK-001) bez zmian. Brak regresji (787 passed, 3 skipped).
- [PR #33](https://github.com/bmateuszideas/fsasm-training-lab/pull/33) (T10) scalony 17.09.2026; merge SHA `ff118da`. T10 dodało `src/fsasm/runtime.py` (`run_plan(state, repository, executor, verifier) -> RuntimeOutcome` — deterministyczny driver E2E prowadzący cały plan (nie tylko TASK-001) przez nowy Domain Core `apply_event` (via `StateRepository.advance` load→apply_event→commit) + `schedule` + kontrolowane `StubExecutor`/`StubVerifier`; bez realnych tools i bez wywołań modelu; pętla: schedule → terminal/BLOCKED/BUSY/WAITING_ON_GATE → TaskReadied (PENDING→READY) → TaskActivated (READY→RUNNING) → execute(stub) → verify(stub) → EvidenceAccepted + TaskVerificationPassed LUB TaskVerificationFailed → TaskRetried/TaskEscalated (gate, NEEDS_HUMAN); Domain Core jedynie przyznaje PASS/retry/gate; `apply_human_gate` aplikuje RETRY_ONCE/ABORT). Dodało zdarzenie domenowe `TaskReadied` (PENDING→READY) w `domain.py`; scheduler kwalifikuje też READY (retry FAILED→READY). Plany 1/3/N osiągają terminalny stan; PASS TASK-001 NIE kończy planu wielozadaniowego. Stara ścieżka M4 bez zmian. Brak regresji (801 passed, 3 skipped).
- [PR #34](https://github.com/bmateuszideas/fsasm-training-lab/pull/34) (T11) scalony 17.09.2026; merge SHA `d1fb7ac`. T11 dodało `src/fsasm/tool_broker.py` (`ToolBroker` — jedyna ścieżka realnego efektu na izolowanym workspace; egzekwuje scope `allowed_files`/`workspace_scope` w kodzie PRZED skutkiem: normalizacja ścieżek, containment workspace root, traversal `..`, symlink escape, limity rozmiaru; każde wywołanie ma `operation_id` i zwraca ustrukturyzowany `ToolObservation` bez nadawania PASS; operacje `list_files`, `read_file`, `search_code`, `apply_patch`, `inspect_changes`; niedozwolony odczyt/zapis nie wywołuje częściowego skutku (atomic temp+replace); model nie dostaje ogólnego terminala). Nowe modele `ToolObservation`/`ToolOperationKind` w `models.py`. `run_checks`/izolacja procesów to T12, nie T11. M1–M4 bez zmian (równoległa v1 ścieżka). Brak regresji (823 passed, 3 skipped).
- M1–M3 historycznie CLOSED. M4 jest demonstratorem z retry i Human Gate, ale **OPEN**; T00–T10 go nie zamykają. M5 **NOT STARTED**. LLMC/fine-tuning odłożone.
- M4 wykonuje tylko `TASK-001` przez deterministyczny stub. Nie ma kompletnego schedulera, Tool Brokera, niezależnego Verifiera artefaktów, realnej pętli model–narzędzie–obserwacja, Gatewaya ani pełnego operacyjnego resume.
- Audyt Astry wskazał G3–G5 oraz szersze luki G1/G2 (revision i niezależne evidence). [Pełny datowany raport](../../../../fsasm-first/docs/reviews/FSASM_V1_ARCHITECTURE_GAP_ANALYSIS_AND_MIGRATION_PLAN_2026-09-17.md). T01 potwierdziło G3–G5 PRESENT; T02 naprawiło G4/G5.

## Jedyny bieżący krok i status decyzji

**Kanon kolejności:** [Kanoniczny program wykonawczy](../../../../fsasm-first/docs/FSASM_RUNTIME_V1_CANONICAL_TODO.md) (T00–T34 + bramki) jest jedynym aktywnym programem kolejności prac. Wczesna [kolejka migracji](../../../../fsasm-first/docs/MIGRATION_BACKLOG_V1.md) jest dokumentem historycznym zastąpionym przez kanoniczne TODO.

**Następny krok: T12 — Kontrolowane run_checks i izolacja procesów.** Zależność: T11 scalone jako `d1fb7ac`. T12 jest `IN IMPLEMENTATION` na branchu `vibe/t12-run-checks-process-isolation-4d91fb` z `d1fb7ac`: rozszerzenie `src/fsasm/tool_broker.py` o `run_checks(kind, args)` (allowlista `CheckKind` → zaufany argv bez shell interpolation, `subprocess` z argv listą `shell=False`, cwd=workspace root, timeout, limit outputu z markerem truncation, env allowlist `PATH`/`PYTHONPATH`/`HOME`/`LANG`/`LC_ALL` — sekrety nie trafiają do child; exe musi być bare name z allowlisty; NUL byte, separatory ścieżek, metaznaki shell, redirect i padded arg odrzucone przed skutkiem; rejestruje exit code, stdout/stderr, czas, `operation_id`/`check_kind`; rozróżnia policy-block / timeout / process-error / negatywny wynik; nie nadaje PASS). Nowy enum `CheckKind` i pola `ToolObservation` (`exit_code`/`stdout`/`stderr`/`duration_ms`/`truncated`/`timed_out`/`check_kind`) w `models.py`; `CheckKind` wyeksportowane z pakietu. Nowe testy `tests/test_v1_tool_broker_processes.py`. M1–M4 bez zmian. Po ewentualnej akceptacji T12: sprawdź nowy HEAD i CI oraz przystąp do T13 (Niezależny Verifier i Evidence Plane). Nie rozpoczynaj T13 ani M5 bez odrębnej zgody.

## Jak aktualizować

Ten plik zawiera tylko aktualny stan, zatwierdzony następny krok, ważne ograniczenia i odnośniki. Przy kolejnym rzeczywistym zdarzeniu (merge, zaakceptowany task, wynik CI, decyzja o milestone) zmień datę, referencyjny SHA i odpowiednie fakty **w tym jednym pliku** w zatwierdzonym PR. Nie twórz równoległego `PROJECT_STATUS.md`, `CURRENT_DEVELOPMENT_ANCHOR.md`, dodatkowego globalnego `HANDOFF.md` ani opisów stanu w README. Sesyjny handoff pojedynczego zadania zapisuj w jego PR/branchu zgodnie z `session-handoff.md`. Fakty implementacji zawsze weryfikuj na Git i testach.
