# FS-ASM — repository handoff / jeden bieżący punkt statusowy

**Aktualizacja dokumentacji:** 17.09.2026. **Zweryfikowany punkt odniesienia:** `Fsasm-experimental` @ `9eda34fd0f85917e807f4cd7b04ff94074b62fac` (merge PR #43, T20 Adapter Mistral + role; T19 scalone jako `9551857` — Gate F w budowie; T18 scalone jako `6af72b5` — Gate E; T17 scalone jako `ab217d7`; T16 scalone jako `53af607`; T15 scalone jako `8ddde10`; T14 scalone jako `e22acde` — Gate D; T13 scalone jako `0406ec5`; T12 scalone jako `75ef09d`; T11 scalone jako `d1fb7ac`; T10 scalone jako `ff118da`; T09 scalone jako `86a503f` całego planu i agregacja Parent/run; Gate C osiągnięta na `56d1997`, T08 scalone jako `76cb8c4`). To datowany snapshot, nie gwarancja aktualnego HEAD. Każda kolejna sesja weryfikuje żywy HEAD, PR i CI przed rozpoczęciem zadania.

## Cel i uprawnienia

[Zatwierdzona architektura v1](../../../../fsasm-first/docs/FSASM_ARCHITEKTURA_RUNTIME_V1_ZATWIERDZONA_2026-09-17.md) jest jedynym kontraktem celu (SHA-256 `56aeeb60c8c700c71dc3d395659127afbf511e1726e0ffd76270c848fd764808`). [Decyzje wdrożeniowe](../../../../fsasm-first/docs/DEPLOYMENT_DECISIONS.md) doprecyzowują profil hybrydowy Workflows i zgodę na jawne dane/telemetrię. `Fsasm-experimental` jest linią integracyjną; `main` pozostaje osobno. Użytkownik + ChatGPT planują/review, Vibe Code Web pisze kod. Nie zmieniaj kanonu, kodu lub milestone'ów na podstawie samego handoffu.

## Potwierdzone na wskazanym punkcie odniesienia

- [PR #23](https://github.com/bmateuszideas/fsasm-training-lab/pull/23) (T00) scalony do `Fsasm-experimental` 17.09.2026; merge SHA `fae7bcf`. Kanoniczny TODO i pojedynczy handoff są w linii integracyjnej.
- [PR #24](https://github.com/bmateuszideas/fsasm-training-lab/pull/24) (T01) scalony 17.09.2026; merge SHA `de302b5`. T01 niezależnie odtworzyło G3, G4 i G5 na HEAD `a470cdd` — wszystkie `PRESENT` (G3: legacy consent reassignable across gates; G4/G5: rejection attribution/drain). Raport reprodukcji przypięty do SHA; bez zmian kodu produkcyjnego.
- [PR #25](https://github.com/bmateuszideas/fsasm-training-lab/pull/25) (T02) scalony 17.09.2026; merge SHA `31d6724`. T02 naprawiło G4/G5 (atrybucja i drain odrzuceń Human Gate) bez regresji RETRY_ONCE/ABORT; późne rejection nie ginie, `None` pozostaje `None`.
- [PR #26](https://github.com/bmateuszideas/fsasm-training-lab/pull/26) (T03) scalony 17.09.2026; merge SHA `a7788a1`. T03 naprawiło G3 (tożsamość zgody Human Gate): runtime wymaga pełnych `run_id`/`task_id`/`gate_id`/`decision_id` na granicy, odrzuca niepełny/obcy/stary/przyszły payload jako `incomplete_payload` przed rezerwacją bramki, zachowuje first-valid-wins i stabilny `decision_id`. **Gate AB osiągnięta:** T00–T03 ACCEPTED, brak warunkowych NOT REQUIRED; pełny pytest zielony na `a7788a1`.
- [PR #27](https://github.com/bmateuszideas/fsasm-training-lab/pull/27) (T04) scalony 17.09.2026; merge SHA `a283bb8`. T04 dodało kompletny model domenowy snapshotu: `TaskBudget`/`RunBudget`/`GateOccurrence`, pola `schema_version`/`revision`/`needs_human_task_ids`/`gate`/`budget`, `allowed_tools`/`accepted_evidence_refs` na `ChildTask`, walidator `validate_register_invariants` (leniwy o stany przejściowe M4), detekcję cykli i 7 deterministycznych helperów tożsamości. `Plan.validate_tasks` rozluźnione z „dokładnie 3” na „min 1”. Brak regresji (660 passed, 3 skipped).
- [PR #28](https://github.com/bmateuszideas/fsasm-training-lab/pull/28) (T05) scalony 17.09.2026; merge SHA `082d13a`. T05 przeniosło macierze przejść/limitów z `transitions.py` do nowego `src/fsasm/domain.py` (jeden autorytet; `transitions.py` re-używa je przez aliasy, API M4 bez zmian); zdefiniowało 13 jawnych zdarzeń domenowych i czyste `apply_event(current_state, event) -> next_state` bez I/O i bez mutacji wejścia. Tylko Domain Core zatwierdza PASS/retry/gate; PASS wymaga `verification_passed=True` + niepustych zaakceptowanych evidence aktualnej próby; jeden aktywny task; `revision` celowo niezmienione (T06). Brak regresji (697 passed, 3 skipped).
- [PR #29](https://github.com/bmateuszideas/fsasm-training-lab/pull/29) (T06) scalony 17.09.2026; merge SHA `56d1997`. T06 dodało `src/fsasm/state_repository.py` (`StateRepository` + `StaleRevisionError`) komponujące `RuntimePersistence` (F3/F4/F5 bez zmian dla M1–M4): `load(run_id)`, `init_snapshot(state)` (revision 0, ścieżka `start_new`) oraz `commit_snapshot(run_id, expected_revision, next_state)` — optymistyczna współbieżność odrzuca stale `expected_revision` (G1), odrzuca niespójny `run_id`, inkrementuje revision raz; `state.json` autorytet zapisywany najpierw, `plan.json` (projekcja) po nim; fault injection dowodzi, że awaria projekcji nie cofa autorytetu i nie daje false PASS. Brak regresji (718 passed, 3 skipped).
- **Gate C osiągnięta:** T04–T06 ACCEPTED, pełny pytest zielony na `56d1997`, kontrolny restart na stubie (nowy `StateRepository`/`RuntimeLifecycle` nad tym samym dyskiem) wraca do zatwierdzonego snapshotu (jeden snapshot = plan + Task Register + gate + counters + evidence refs; jedna czysta operacja `apply_event` + jeden commit; brak drugiego źródła statusów).
- [PR #30](https://github.com/bmateuszideas/fsasm-training-lab/pull/30) (T07) scalony 17.09.2026; merge SHA `b04b1f8`. T07 dodało `src/fsasm/runtime_lifecycle.py` (`RuntimeLifecycle.start_new` rezerwuje NOWY run F4 i pisze pierwszy snapshot revision 0, odmawia nadpisania istniejącego runu/celu; `resume` ładuje i waliduje zatwierdzony snapshot, zwraca `(state, revision)`, nie obiecuje reconcile — T23) oraz `StateRepository.advance(run_id, expected_revision, event)` (adapter activities `load → apply_event → commit_snapshot`); nieaktualna activity (stale `expected_revision`) odrzucona i nie nadpisuje nowszego stanu; technical retry (zdarzenie nie-attempt) nie zwiększa `task_attempt`. M1–M4 nadal używają imperatywnej ścieżki `commit_run_state` (nie usuwamy jej do T27). Brak regresji (735 passed, 3 skipped).
- [PR #31](https://github.com/bmateuszideas/fsasm-training-lab/pull/31) (T08) scalony 17.09.2026; merge SHA `76cb8c4`. T08 dodało `src/fsasm/task_compiler.py` (`compile_plan(goal_input, proposal) -> Plan` — jedyna granica zamieniająca niezaufany `PlannerProposal` w runtime-owned `Plan`; nadaje runtime-owned IDs, Parent/Child, zależności, statusy, próby, scope i limity kapowane przez `GoalInput.constraints` jako jedyny źródło uprawnień; odrzuca pustą propozycję, plan-size overflow, duplikaty, złe/brakujące/cykliczne zależności, rozszerzenie uprawnień i nieznany verification_type PRZED utworzeniem runu; zwraca czysty `Plan` bez typów transportowych). `GoalInput` rozszerzone o `RunConstraints`; `PlannerProposal` rozluźnione z „dokładnie 3” na „min 1” (semantyczne wejście). M1–M4 bez zmian. Brak regresji (765 passed, 3 skipped).
- [PR #32](https://github.com/bmateuszideas/fsasm-training-lab/pull/32) (T09) scalony 17.09.2026; merge SHA `86a503f`. T09 dodało `src/fsasm/scheduler.py` (`schedule(state) -> ScheduleResult` — czyste zapytanie nad `RunState`, bez mutacji/I/O/nadawania PASS; wybiera deterministycznie kwalifikujący się Child Task (najniższy sequence, tie-break task_id) po spełnieniu wszystkich zależności; utrzymuje najwyżej jeden aktywny task — `BUSY` gdy `active_task_id` ustawione; rozróżnia `ALL_COMPLETE` / `BLOCKED` / `WAITING_ON_GATE` (run NEEDS_HUMAN) / `BUSY`; `all_required_tasks_complete` liczy Parent/run ze wszystkich wymaganych Child Tasks — PASS TASK-001 NIE kończy planu wielozadaniowego). M1–M4 `find_next_ready_task_activity` (hardcoded TASK-001) bez zmian. Brak regresji (787 passed, 3 skipped).
- [PR #33](https://github.com/bmateuszideas/fsasm-training-lab/pull/33) (T10) scalony 17.09.2026; merge SHA `ff118da`. T10 dodało `src/fsasm/runtime.py` (`run_plan(state, repository, executor, verifier) -> RuntimeOutcome` — deterministyczny driver E2E prowadzący cały plan (nie tylko TASK-001) przez nowy Domain Core `apply_event` (via `StateRepository.advance` load→apply_event→commit) + `schedule` + kontrolowane `StubExecutor`/`StubVerifier`; bez realnych tools i bez wywołań modelu; pętla: schedule → terminal/BLOCKED/BUSY/WAITING_ON_GATE → TaskReadied (PENDING→READY) → TaskActivated (READY→RUNNING) → execute(stub) → verify(stub) → EvidenceAccepted + TaskVerificationPassed LUB TaskVerificationFailed → TaskRetried/TaskEscalated (gate, NEEDS_HUMAN); Domain Core jedynie przyznaje PASS/retry/gate; `apply_human_gate` aplikuje RETRY_ONCE/ABORT). Dodało zdarzenie domenowe `TaskReadied` (PENDING→READY) w `domain.py`; scheduler kwalifikuje też READY (retry FAILED→READY). Plany 1/3/N osiągają terminalny stan; PASS TASK-001 NIE kończy planu wielozadaniowego. Stara ścieżka M4 bez zmian. Brak regresji (801 passed, 3 skipped).
- [PR #34](https://github.com/bmateuszideas/fsasm-training-lab/pull/34) (T11) scalony 17.09.2026; merge SHA `d1fb7ac`. T11 dodało `src/fsasm/tool_broker.py` (`ToolBroker` — jedyna ścieżka realnego efektu na izolowanym workspace; egzekwuje scope `allowed_files`/`workspace_scope` w kodzie PRZED skutkiem: normalizacja ścieżek, containment workspace root, traversal `..`, symlink escape, limity rozmiaru; każde wywołanie ma `operation_id` i zwraca ustrukturyzowany `ToolObservation` bez nadawania PASS; operacje `list_files`, `read_file`, `search_code`, `apply_patch`, `inspect_changes`; niedozwolony odczyt/zapis nie wywołuje częściowego skutku (atomic temp+replace); model nie dostaje ogólnego terminala). Nowe modele `ToolObservation`/`ToolOperationKind` w `models.py`. `run_checks`/izolacja procesów to T12, nie T11. M1–M4 bez zmian (równoległa v1 ścieżka). Brak regresji (823 passed, 3 skipped).
- [PR #35](https://github.com/bmateuszideas/fsasm-training-lab/pull/35) (T12) scalony 17.09.2026; merge SHA `75ef09d`. T12 rozszerzyło `src/fsasm/tool_broker.py` o `run_checks(kind, args)` (allowlista `CheckKind` → zaufany argv bez shell interpolation, `subprocess` z argv listą `shell=False`, cwd=workspace root, timeout, limit outputu z markerem truncation, env allowlist `PATH`/`PYTHONPATH`/`HOME`/`LANG`/`LC_ALL` — sekrety nie trafiają do child; exe musi być bare name z allowlisty; NUL byte, separatory ścieżek, metaznaki shell, redirect i padded arg odrzucone przed skutkiem; rejestruje exit code, stdout/stderr, czas, `operation_id`/`check_kind`; rozróżnia policy-block / timeout / process-error / negatywny wynik; nie nadaje PASS). Nowy enum `CheckKind` i pola `ToolObservation` (`exit_code`/`stdout`/`stderr`/`duration_ms`/`truncated`/`timed_out`/`check_kind`) w `models.py`; `CheckKind` wyeksportowane z pakietu. M1–M4 bez zmian. Brak regresji (851 passed, 3 skipped).
- [PR #36](https://github.com/bmateuszideas/fsasm-training-lab/pull/36) (T13) scalony 17.09.2026; merge SHA `0406ec5`. T13 dodało `ArtifactVerifier` + `VerifyRequest` w `src/fsasm/verifier.py` — niezależny Verifier czyta RZECZYWISTY artefakt przez `ToolBroker.inspect_changes` oraz rzeczywisty wynik `run_checks` (`ToolObservation`), NIE deklarację Executora; wiąże wynik z pełną tożsamością próby (`run_id`/`task_id`/`attempt`/`operation_id`/`artifact_id`/`check_identity`) i produkuje `EvidenceRecord` dla aktualnej próby; tylko Domain Core zatwierdza PASS (wymaga `verification_passed=True` + niepustych `accepted_evidence_refs` aktualnej próby — F8 zachowane w `domain.py`). Negatywny check, brak evidence, stara próba, obcy artefakt i „DONE” bez skutku blokują PASS (reproduktor G2). Rozszerzenie `EvidenceRecord` (`attempt`/`operation_id`/`artifact_id`/`check_identity`) i `VerificationResult` (`attempt`/`operation_id`/`artifact_id`/`evidence_refs` + `check_identity` na `VerificationCheck`) w `models.py` — pola opcjonalne, M1–M4 bez zmian. Nowe testy `tests/test_v1_verifier_artifacts.py`. Brak regresji (866 passed, 3 skipped).
- [PR #37](https://github.com/bmateuszideas/fsasm-training-lab/pull/37) (T14) scalony 17.09.2026; merge SHA `e22acde`. T14 dodało `src/fsasm/real_effects.py` (`run_plan_real(state, repository, broker, verifier, executor) -> RealEffectsOutcome`) — deterministyczny E2E prowadzący CAŁY plan przez REALNE izolowane skutki: kontrolowany `ControlledToolExecutor` modyfikuje realny fixture przez `ToolBroker` (patch + `run_checks`), `ArtifactVerifier` czyta rzeczywisty artefakt i check (NIE deklarację), evidence utrwalane `save_evidence` PRZED snapshotem, Domain Core zatwierdza PASS przez `StateRepository.advance`; PASS nie może pochodzić z tekstu stuba. Scenariusze: fałszywa deklaracja bez zmiany (G2), policy block, negatywny check → retry → PASS na próbie 2, stare evidence (TaskActivated czyści refs), pełny sukces, plany 2/3-task. **Gate D osiągnięta:** T11–T14 ACCEPTED, pełny pytest zielony na `e22acde` (878 passed, 3 skipped) — pełny DAG sekwencyjnie wykonywany, narzędzia egzekwują scope, PASS z rzeczywistego artefaktu aktualnej próby, każdy accepted result ma integralne evidence refs w jednym snapshocie.
- [PR #38](https://github.com/bmateuszideas/fsasm-training-lab/pull/38) (T15) scalony 17.09.2026; merge SHA `8ddde10`. T15 dodało `src/fsasm/context.py` (`ContextBuilder.build(state) -> TaskContext`) składający ograniczony, z provenance śledzony kontekst dla jednej próby Child Task — cel, acceptance criteria, ograniczenia, allowed files/tools, istotne źródła, poprzednie observations/verification, context budget. Pierwsza wersja: jawne ścieżki czytane przez `ToolBroker` (scope egzekwowany), symbol/import search po zatwierdzonym workspace scope (targeted fragmenty bez whole-file dump), proste dependency outputs. Każdy fragment ma mandatory provenance; plik spoza scope i whole-repo dump nie wchodzą; budżet znakowy z cap per-fragment. Nowe modele `TaskContext`/`ContextFragment` w `models.py` (wyeksportowane, M1–M4 bez zmian). Brak regresji (896 passed, 3 skipped).
- [PR #39](https://github.com/bmateuszideas/fsasm-training-lab/pull/39) (T16) scalony 17.09.2026; merge SHA `53af607`. T16 dodało `src/fsasm/model_types.py` (żądanie `ModelRequest` + odpowiedzi `ToolCall`/`FinalResponse`/`EscalationRequest`/`InvalidResponse`, znormalizowane błędy `ModelError`/`ModelErrorKind`, `ModelUsage`, budżety `ModelCallBudget`/`BudgetUsage`) oraz `src/fsasm/gateway.py` (`ModelGateway.generate(request) -> ModelResult` — jeden interfejs niezależny od dostawcy; `ModelBackend` Protocol; deterministyczny `ScriptedBackend` pozwalający zaplanować kolejne odpowiedzi i błędy; ścisły `parse_scripted_output` — błędny format ZAWSZE `InvalidResponse`, nigdy milczący sukces; błąd transportu ZAWSZE `ModelError`, nigdy domena). Parser nie myli błędnego formatu z tool callem ani final response. Brak typów dostawcy w domenie. Nowe testy `tests/test_v1_gateway_contract.py`. Brak regresji (937 passed, 3 skipped).
- [PR #40](https://github.com/bmateuszideas/fsasm-training-lab/pull/40) (T17) scalony 17.09.2026; merge SHA `ab217d7`. T17 dodało `src/fsasm/executor_loop.py` (`ExecutorLoop.run` — pętla model → Tool Broker → ToolObservation → model aż do jawnego terminalnego outcome dla jednej `task_attempt`; wiele `agent_step`/tool calls/model calls w jednej próbie rozdzielone REALNYMI efektami; budżety `ModelCallBudget`/`BudgetUsage` egzekwowane PRZED następnym skutkiem → `STEP_LIMIT_REACHED`; nowy `ExecutorOutcomeReason` w `model_types.py` — `COMPLETED` (prośba o Verification NIE PASS), `NEEDS_INFORMATION`, `ESCALATION_REQUESTED`, `STEP_LIMIT_REACHED`, `TOOL_ERROR`, `POLICY_BLOCKED`; `FinalResponse` = prośba o Verification NIE PASS; `ModelError` → `TOOL_ERROR` z `transport_error` bez niejawnej nowej próby; `InvalidResponse` re-ask w budżecie; undeclared/out-of-scope tool → `POLICY_BLOCKED` bez efektu; observation jako TOOL-role message; pętla nie mutuje `RunState`). M3/M4 `ExecutorStub` bez zmian (równoległa v1 ścieżka). Brak regresji (959 passed, 3 skipped).
- [PR #41](https://github.com/bmateuszideas/fsasm-training-lab/pull/41) (T18) scalony 17.09.2026; merge SHA `6af72b5`. T18 dodało `src/fsasm/retry.py` (`classify_attempt(outcome, task, transport_budget, verification_result) -> RetryVerdict` rozdziela trzy miary retry: transport error → `TRANSPORT_RETRY` tej samej próby bez bumpa `task_attempt` (bounded `TransportRetryBudget`); merytoryczna verification FAIL → `MERYTORYCZNA_RETRY` z konkretnym feedbackiem dla Context Builder kolejnej próby; wyczerpanie `max_attempts` → `ESCALATE` do Human Gate; terminalne non-retry outcome → `STOP`; `build_retry_feedback` — concrete prior observations + prior verification message + aktualny artefakt czytany przez Broker (scope egzekwowany); retry layer NIE nadaje PASS ani przejścia — zwraca decyzję dla drivera do aplikacji przez Domain Core). `ExecutorAttemptOutcome.transport_error` w `executor_loop.py` rozróżnia transport error od merytorycznego FAIL. Nowe testy `tests/test_v1_task_retry_feedback.py` (15). **Gate E osiągnięta:** T15–T18 ACCEPTED, pełna pętla na kontrolowanym modelu (context ograniczony+provenance, Gateway normalizuje, Executor wielokrokowy, retry merytoryczne/techniczne/agent_steps oddzielne).
- [PR #42](https://github.com/bmateuszideas/fsasm-training-lab/pull/42) (T19) scalony 17.09.2026; merge SHA `9551857`. T19 dodało `src/fsasm/adapters/local.py` (`LMStudioAdapter` — jeden wybrany protokół lokalny, LM Studio OpenAI-compatible `/v1/chat/completions`; implementuje `ModelBackend`; tłumaczy `ModelRequest`→wire httpx, parsuje→`ModelResult`; NIE trzyma reguł domenowych — brak retry/PASS/routing/scope; strict: malformed tool call→`InvalidResponse` nigdy milczący `ToolCall`; `[VERIFY]`→`FinalResponse` (prośba o Verification NIE PASS), `[ESCALATE ...]`→`EscalationRequest`, nieprzetwarzalny→`InvalidResponse`; transport errors→`ModelError` z mapowaniem statusu→`ModelErrorKind`: 408→TIMEOUT, 429→RATE_LIMITED, 401/403→AUTHENTICATION, 400→BAD_REQUEST, 5xx/conn→UNAVAILABLE, non-JSON/empty choices→PROTOCOL_ERROR; usage parsowane gdy obecne, absent→None honest; endpoint bound do loopback `127.0.0.1:1234`). `httpx` w lockfile. Testowane na httpx `MockTransport` bez realnego 7B i bez sieci. Nowe testy `tests/test_v1_local_adapter_contract.py` (30). Brak regresji (1004 passed, 3 skipped).
- [PR #43](https://github.com/bmateuszideas/fsasm-training-lab/pull/43) (T20) scalony 17.09.2026; merge SHA `9eda34f`. T20 dodało `src/fsasm/adapters/mistral.py` (`MistralAdapter` — JEDEN transportowy adapter dla Mistral API z konfigurowanymi **logicznymi rolami** `MistralRole` PLANNER/API_A/API_B, bez zakładania konkretnych nazw modeli — model per rola z konfiguracji T29; implementuje `ModelBackend`; ta sama instancja obsługuje Plannera i role A/B jednym kontraktem; strict: malformed tool call→`InvalidResponse`, `[VERIFY]`→`FinalResponse`, `[ESCALATE consultation|handover]`→`EscalationRequest`, invalid→`InvalidResponse`; transport errors→`ModelError` z mapowaniem statusu identycznym jak local adapter; usage parsowane honest; brak retry/PASS/scope/routing w adapterze). Modyfikacja `planner_activities.py`: `build_planner_gateway`/`plan_with_mistral_gateway` przepodłączają Plannera przez wspólny `ModelGateway`+`MistralAdapter` (rola PLANNER) bez zmiany własności Task Compilera (`assemble_plan` kompiluje propozycję); aktywna activity `plan_activity` bez zmian sygnatury (M1–M4/Temporal validation nietknięte). Testowane na httpx `MockTransport` bez płatnych/live wywołań. Nowe testy `tests/test_v1_mistral_adapter_contract.py` (46). Brak regresji (1050 passed, 3 skipped).
- **Decyzja §6 „Lokalny protokół” rozstrzygnięta przed T19:** zatwierdzono **LM Studio** (OpenAI-compatible `/v1/chat/completions` z tool callingiem i usage, GUI-driven, GGUF Q4) jako jedyny protokół lokalnej inferencji; potwierdzenie przy T29 na laptopie. Adapter T19 implementuje ten protokół.
- M1–M3 historycznie CLOSED. M4 jest demonstratorem z retry i Human Gate, ale **OPEN**; T00–T18 go nie zamykają. M5 **NOT STARTED**. LLMC/fine-tuning odłożone.
- M4 wykonuje tylko `TASK-001` przez deterministyczny stub. Nie ma kompletnego schedulera, Tool Brokera, niezależnego Verifiera artefaktów, realnej pętli model–narzędzie–obserwacja, Gatewaya ani pełnego operacyjnego resume.
- Audyt Astry wskazał G3–G5 oraz szersze luki G1/G2 (revision i niezależne evidence). [Pełny datowany raport](../../../../fsasm-first/docs/reviews/FSASM_V1_ARCHITECTURE_GAP_ANALYSIS_AND_MIGRATION_PLAN_2026-09-17.md). T01 potwierdziło G3–G5 PRESENT; T02 naprawiło G4/G5.

## Jedyny bieżący krok i status decyzji

**Kanon kolejności:** [Kanoniczny program wykonawczy](../../../../fsasm-first/docs/FSASM_RUNTIME_V1_CANONICAL_TODO.md) (T00–T34 + bramki) jest jedynym aktywnym programem kolejności prac. Wczesna [kolejka migracji](../../../../fsasm-first/docs/MIGRATION_BACKLOG_V1.md) jest dokumentem historycznym zastąpionym przez kanoniczne TODO.

**Następny krok: T21 — Model Router, consultation i handover.** Zależność: T20 (scalone jako `9eda34f`) oraz zatwierdzona konfiguracja limitów logicznych (§6 — wartości konserwatywne/konfigurowalne). T21 jest `IN IMPLEMENTATION` na branchu `vibe/t21-model-router-handover-4d91fb` z `9eda34f`: nowy `src/fsasm/router.py` (`ModelRouter` — deterministyczna warstwa decyzyjna consultation/handover, NIE adapter, brak transportu modelu/wywołań I/O; `route(escalation, state, current_scope) -> RouterDecision`: `LocalRoute` domyślna dla normalnego Child Task bez eskalacji; `ConsultRoute` — consultation zwraca wskazówkę do kontekstu bieżącej próby, właściciel wykonania NIEzmieniony, NIE dodaje `task_attempt`, NIE dotyka scope; `HandoverRoute` — jawny transfer do innego adaptera, scope ten sam lub węższy (nigdy szerszy — router nie wymyśla scope), własny budżet, zmienia właściciela; `RouteToGate` — wyczerpane limity (max_escalations_per_run, max_handovers_per_task, max_consultations_per_task, max_escalation_cost) → zatwierdzona ścieżka gate/stop, NIE nieskończona pętla; `RouteRejected` — ping-pong (handover do obecnego właściciela lub z powrotem do roli, z której przyszło), niedozwolone przejście/cel, obcy rodzaj; `RouterState` immutable, `apply_decision` zwraca NOWY stan bez mutacji wejścia; `ask_expert`/consultation NIE obchodzi scope ani nie dodaje prób). Konkretne nazwy modeli A/B z T29 — router ich nie zakłada. Po ewentualnej akceptacji T21: sprawdź nowy HEAD i CI oraz przystąp do T22 (Human Gate jako trwała semantyka Domain Core). Nie rozpoczynaj T22 ani M5 bez odrębnej zgody.

## Jak aktualizować

Ten plik zawiera tylko aktualny stan, zatwierdzony następny krok, ważne ograniczenia i odnośniki. Przy kolejnym rzeczywistym zdarzeniu (merge, zaakceptowany task, wynik CI, decyzja o milestone) zmień datę, referencyjny SHA i odpowiednie fakty **w tym jednym pliku** w zatwierdzonym PR. Nie twórz równoległego `PROJECT_STATUS.md`, `CURRENT_DEVELOPMENT_ANCHOR.md`, dodatkowego globalnego `HANDOFF.md` ani opisów stanu w README. Sesyjny handoff pojedynczego zadania zapisuj w jego PR/branchu zgodnie z `session-handoff.md`. Fakty implementacji zawsze weryfikuj na Git i testach.
